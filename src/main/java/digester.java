import java.io.IOException;
import java.nio.charset.StandardCharsets;
import java.security.MessageDigest;
import java.security.NoSuchAlgorithmException;
import java.util.Base64;

public class digester {
	

	public static void diges(String text2) throws IOException, NoSuchAlgorithmException {
		
		/*String asura = "";
		MessageDigest md = MessageDigest.getInstance("MD5");
		byte[] asura1 = md.digest(asura.getBytes(StandardCharsets.UTF_8));
		String encoded = Base64.getEncoder().encodeToString(asura1);
		System.out.println(encoded);*/
		
		
	
	
	
	}
	private void synesthesia() {
		
	}
}
